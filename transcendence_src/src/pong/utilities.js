// import { deriveZspeed } from "./math";

export function speedUp(speed, increment)
{
    return (speed += increment);
}

// export function setSpeed(speed, angle, speedX, speedZ)
// {
//     deriveXSpeed(speed, angle, speedX);
//     deriveZspeed(speed, angle, speedZ);
// }